### 90860 plant.tree
# depth, options

depth p0;
options p1;

r0:=k==0;

draw(r0, 90861);

_ @1010;
_ @sys.node;
_ @sys.os;
_ @os.basic;
_ @os.util0;
_ @os.apps;
_ @neo.ui;

_ @91000;
_ @twobin.font.inc;

_ @os.basic.paint;

_ @1100;
#_ @sys.touch;
#_ @sys.events;
_ @sys.obj;
#_ @sys.llog;
_ @util.dma.std;

plant.node {
 params {
  outputs.count 0;
  draw.type 1;
 };
 output.points {array{x;y;};};
};

 random.xyr { 
  # buf0,bj0,bi0, n, w,h, store.fn(buf0,bj0,bi0, i, x,y,r)
  buf0 _0; bj0 _1; bi0 _2; 
  n _3; w _4; h _5; 
  store.fn _6(__);
  i:=0;
  while (i<n) {
   x rnd(u0)*w;
   y rnd(u0)*h;
   r rnd(u0)*100-50;
   _6(buf0,bj0,bi0, i, x,y,r);
   i+=1;
  };
 };
 random.xyr0 {
  store buffer.set3(u0, _0,_1,_2+_3*3, _4,_5,_6); 
  random.xyr(__, store);
 };

get.outputs.count 3;

child.points0 r2;

cp0 child.points0;

#if (depth==0) {
# h:=rnd(u0)*10+100;
# w:=rnd(u0)*20+20;
#};

_ @sys.os;
_ @os.basic;
_ @os.basic.ui;


created r3;

max.depth 5;

if ((h>0)*(created==0)) {
 created:=1;
 n:=get.outputs.count;

 cp0:=buffer.new(u0, 10*3,1000);

 random.xyr0(cp0,0,0, n, w,h);
 t.max 1;
 r7:=round(rnd(u0)*t.max);

# r7:=0;
# r7:=2;
# r7:=2;
 r7:=3;

 sn0 d1;
# new.ui.shader.node(sn0, 0,0,1, w*4,h*4, 1, 70014,28155); # grid 
# new.ui.shader.node(sn0, 0,0,1, w*4,h*4, 1, 70014,28148);
# new.ui.shader.node(sn0, 0,0,1, w*2,h*2, 1, 70014,28132);
# new.ui.shader.node(sn0, 0,0,1, w*2,h*2, 1, 70014,28150);
# new.ui.shader.node(sn0, 0,0,1, w*2,h*2, 1, 70014,28165);
# new.ui.shader.node(sn0, -50,-50,1, 100,100, 1, 70014,28171);

#new.ui.shader.node(sn0, 0,0,1, 100,100, 1, 70014,28155);
#new.ui.shader.node(sn0, 0,0,1, 100,100, 1, 70014,28132);
new.ui.shader.node(sn0, 0,0,1, 100,100, 1, 70014,75349);
a {
#new.ui.shader.node(sn0, 0,0,1, 100,100, 1, 70014,28130);
new.ui.shader.node(sn0, -50,-50,1, 100,100, 1, 70014,28144);
#new.ui.shader.node(sn0, -50,-50,1, 100,100, 1, 70014,28132);
#new.ui.shader.node(sn0, -25,-25,1, 50,50, 1, 70014,28142);
new.ui.shader.node(sn0, -100,-100,1, 200,200, 1, 70014,28148);
new.ui.shader.node(sn0, -50,-50,1, 100,100, 1, 70014,28150);
};


#a;

# new.ui.shader.node(sn0, -50,-50,1, 100,100, 1, 70014,28170);

 plant0 d0;

 if (depth<max.depth) {
  i:=0; while (i<n) {
   new.plant.tree(plant0, 0,h,1, w,h, depth+1, options);
   r d1;
   r:=buffer.get(u0, cp0,0,i*3+2);
   node.prop.r.s(u0, plant0, r);
   i+=1;
  };
 };
};

#s:=(max.depth-depth+1)/max.depth;
s:=(max.depth+0.5*depth+1)/max.depth;
r+=0.1*dt;
#watch(depth);

#r:=sin(t*0.1)*50;

t+=dt;

k:=1;

### 90861 plant.tree.draw

t0 t*10;

t {
 rect 0;
 circle 1;
 max;
};

get.draw.type r7;

depth p0;

psize(u0,3);

color(u0,0.1+sin(t0*0.21),0.9,0.1+sin(t0*0.1),0.9);

 fill.type 6;

#type l1;
type get.draw.type;
#type 0;
if (type==t.rect) {
 sw 1;
 sh 1;
 rect(u0, fill.type, 0-w*sw/2,0, w*sw,h*sh);
 color(u0,0.9+sin(t0*0.521),0.0,0.1+sin(t0*0.1),0.9);
# rect(u0, fill.type, 0-w*sw/2+5,0+5, w*sw-10,h*sh-10);
};
if (type==t.circle) {
 sr 1;
 circle(u0, fill.type, 0,h*sr/2, h/2*sr, 0, 20, 0);
# color(u0,0.1+sin(t0*0.21),0.9,0.9+sin(t0*0.1),0.9);
 color(u0,0.9+sin(t0*0.521),0.0,0.1+sin(t0*0.1),0.9);
 circle(u0, fill.type, 0,h*sr/2, h/2*sr/2, 0, 20, 0);
};


#color(u0,1,0,0,1);
#rect(u0, type, 0,0,500,500);

psize(u0, 2);
color(u0,1,1,1,1);
circle(u0, 3, 0,0, 10, 0, 20, 0);

