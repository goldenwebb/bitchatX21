### 900792 basic.ui.tests.node2

_ @4;
_ @900776;

filter0 l3;
filter.self filter0;

test.shader.node2 {
 vsh 150004;
 fsh 150002;

 _ @basic.ui.shader.node2.inc;

 filter.new;
 filter.init(0,vsh,fsh);
# filter.add.texture(tex0);
# filter.add.f1(0.123);

 cid0 l0;
 new.ui.shader.node2(cid0, 0,0,1, w,h, 0, filter0);
};

test.shader.node2.double {
 vsh 150004;
 fsh0 150002;
 fsh1 150003;

 f0 l4;
 f1 l5;

 _ @basic.ui.shader.node2.inc;

 filter.new;
 filter.init(0,vsh,fsh0);
# filter.add.texture(tex0);
# filter.add.f1(0.123);

 f0:=filter.self;

# s 8;
 s 1;
# s 2;
# s 0.5;

 nid0 r2;
 new.ui.shader.node2(nid0, 0,0,s, w,h, 0, filter0);

 tex0 node.value.g(u0, nid0,0);

 filter.new;
 filter.init(0,vsh,fsh1);
 filter.add.texture(tex0);

 f1:=filter.self;

 nid1 r3;
 new.ui.shader.node2(nid1, 0,500,s+40, w,h, 0, filter0);

 tex1 node.value.g(u0, nid1,0);

 filter.self:=f0;
 filter.add.texture(tex1);
};

#ui.value1(l12,10,10,2,filter0);

r0:=k==0;

draw(r0, 900793);

#w:=32;
#h:=32;
#w:=500;
#h:=500;
w:=256*4;
h:=256*4;

#w:=1256;
#h:=1256;

if (r0) {
# test.shader.node2;
 test.shader.node2.double;
};

test.shader.node2.double.step {
 filter.self:=f0;
 filter.fsh.s(150003);

 copy {
  filter.fsh.s(150006);
  filter.self:=f1;
  filter.fsh.s(150006);
 };
 if (mod(r1,10)!=0) {
  copy;
 };

 if (r1==5) {
 # filter.fsh.s(150005);
 # filter.fsh.s(28512);
 # filter.fsh.s(28477);
 # filter.fsh.s(28468);
 # filter.fsh.s(28460);
  filter.fsh.s(28450);

 
 };

};


# next.turn
if (r1>=1) {
 test.shader.node2.double.step;
};

r1+=1;



x:=100;
y:=100;

t+=dt;

k:=1;

### 900793 basic.ui.tests.node2.draw

psize(u0, 1);
color(u0, 0.3,0.0,0.5, 1);
circle(u0, 6, 60,70, 100, 0, 20, 0);
color(u0, 1,0.2,0, 0.7);
circle(u0, 6, 60,70, 45+5*sin(cos(t)*t), 0, 20, 0);
color(u0, 1,1,0, 0.7);
circle(u0, 6, 60,70, 15+5*sin(cos(t)*t), 0, 20, 0);

circle(u0, 1, 60,70, 60, sin(t*1)*1, 50, 0);
color(u0, 1,1,0, 1);
circle(u0, 0, 60,70, 55, sin(-t*0.7)*0.5, 20, 0);
#rect(u0, 6, 0,0,500,500);
