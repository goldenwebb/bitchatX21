### ui0.xstack.common
# VIEW.OWNER

# XSTACK COMMON [

XSTACK.DBG 0;

mode0 p0;

_ @easy.start.new;
_ @4;

_ @sys.events;
_ @sys.llog;

_ @ui0.xstack.inc;

if (XSTACK.DBG) {
 draw.fid(ui0.xstack.draw);
};

# Flags
sync.width 0;
sync.height 0;
auto.width 0;
auto.height 0;

# XSTACK COMMON ]
# VISIBLE [

view1 = node.get.v0(nid, VIEW.OWNER, 0); #

#view1=node.parent(u0, nid);
vr.x 0;
vr.y 0;
#vr.w 500;
#vr.h 500;
vr.w=node.get.w(u0, view1);
vr.h=node.get.h(u0, view1);

#vr.w=node.get.w(u0, view1);
#vr.h=node.get.h(u0, view1);

check.visible {
 x0 0;
 y0 0;

 node.transform.point(u0, view1, nid, x0,y0);
 x0=o0;
 y0=o1;
 node.transform.point(u0, view1, nid, w,h);
 x1=o0;
 y1=o1;

 0+(y0<=vr.h)*(y1>=vr.y) * (x0<=vr.w)*(x1>=vr.x);
};

visible 0;

if (view1>0) {
 visible:=check.visible(vr);
 #visible:=0;
 node.set.visible(u0, nid, visible);
};

# VISIBLE ]

#lbl0 0; val0 0; ui.value4(lbl0,val0,0,100,1, W, 111);
#node.set.xy(u0, val0, 100,100);
#lbl0 0; val0 0; ui.value4(lbl0,val0,0,0,1, H, h);
#lbl0 0; val0 0; ui.value4(lbl0,val0,0,0,1, A.VSTACK.h, layout.nodes.y0/2);

#lbl0 0; val0 0; ui.value4(lbl0,val0,0,0,1/3, S.view1, view1);

# DBG LLOG [

_ @sys.llog;

#llog.b(XSTACK000);
#llog.erase(r0, 0);

#qq 0;
#qq+=1;
#if (qq==10) {
# llog.pv(GOOD, 1);
# llog.po(OK, 0, OK1);
# llog.pv2(NID, view1, nid);
# llog.pv2(WH, w, h);
#};

#llog.e;

# DBG LLOG ]

### ui0.xstack.draw

color(u0, 0.5,0.5,0,0.5);
#rect(u0, 6, 0,0,w,h);
#color(u0, 1,0,0,0.9);
#rect(u0, 2, 0,0,w,h);
