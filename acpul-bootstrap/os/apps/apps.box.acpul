### 91220 os.apps.box
# mode, app, param...

mode p0;
app p1;
param0 p2;

r0:=k==0;

draw(r0, 91221);

_ @1010;
_ @sys.node;
_ @sys.os;
_ @os.basic;
_ @os.util0;
_ @os.apps;
_ @neo.ui;

if (mode==0) {
 node.new.this.ex(r0, app, param0);
};
if (mode==1) {
 screen.size0;
 center.up;

 node.new.this.ex(r0, app, param0);
};

t+=dt;

#s:=1+0.5*sin(t);

k:=1;

### 91221 os.apps.box.draw

color(u0, 0.2,0.5,0.5+0.5*sin(t),0.5);

rect(u0, 6, 0,0,150,25);
#line(u0, 2, 0,0,150,30);
#line(u0, 2, 0,30,150,0);

color(u0, 0.9,0.5,0.5+0.5*sin(t),0.5);

psize(u0, 2);

_ @90100;
_ @1010;
_ @sys.os;
_ @twobin.font.inc;

twobin.font.scale l3;
twobin.font.cursor.x l0;
twobin.font.cursor.y l1;
twobin.font.gap 10;

twobin.font.def8x4.txt0 {
 begin;
 scale:=3;
 A;P;P;S;
 B;O;X;
};
twobin.font.def8x4.txt0;
